@PACKAGE_INIT@

set(USE_MPIA_LA @MPIA_LA@)
if(USE_MPIA_LA)
	find_package(locality_aware)
endif(USE_MPIA_LA)

set(USE_MPIA_PC @MPIA_PC@)
if(USE_MPIA_PC)
	find_package(MPIPCL)
endif(USE_MPIA_PC)

set(USE_MPIA_ST @MPIA_ST@)
if(USE_MPIA_ST)
	find_package(stream_triggering)
endif(USE_MPIA_ST)


